/**
 * String array containing the routes a user
 * can visit without being logged in.
 * Visiting a routes not defined in this
 * directory will automatically redirect
 * the user to the login page.
 */
export const publicRoutes = ['/', '/login', '/register'];
